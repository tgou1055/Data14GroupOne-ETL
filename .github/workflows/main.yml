name: Deploy CloudFormation Stacks

on:
  workflow_dispatch:
    inputs:
      region:
        description: "AWS Region"
        required: true
        default: "ap-southeast-2"
      scriptsBucket:
        description: "S3 bucket name where scripts will be uploaded"
        required: true
        default: "data14group1-scripts"

jobs:
  cfn-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Configure AWS credentials
        id: creds
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ github.event.inputs.region }}

      - name: Run upload script
        run: sh scripts/upload_to_s3.sh ${{ github.event.inputs.scriptsBucket }}


      # - name: Deploy CloudFormation Stack
      #   id: deploy-stack
      #   uses: aws-actions/aws-cloudformation-github-deploy@v1.0.3
      #   with:
      #     name: deploy-stack
      #     template: cloudformation/root.yml
      #     parameter-overrides: scriptsBucket=${{ github.event.inputs.scriptsBucket }}
      #     capabilities: CAPABILITY_IAM,CAPABILITY_NAMED_IAM
      #     no-fail-on-empty-changeset: "1"
